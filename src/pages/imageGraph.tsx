import ImageContainer from "@/components/searchResult/ImageContainer";
import React from "react";

const imageGraph = () => {
  return (
    <div>
      <ImageContainer />
    </div>
  );
};

export default imageGraph;
